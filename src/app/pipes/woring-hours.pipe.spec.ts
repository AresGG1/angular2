import { WorkingHoursPipe } from './woring-hours.pipe';

describe('WoringHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkingHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
