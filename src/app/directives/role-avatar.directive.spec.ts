import { RoleAvatarDirective } from './role-avatar.directive';

describe('RoleAvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleAvatarDirective();
    expect(directive).toBeTruthy();
  });
});
